<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="FrontUserMapper">

	<insert id="save" parameterType="pd">
		insert into WEB_USER
			(ID,FIRST_NAME,LAST_NAME,EMAIL,PASSWORD,CREATE_TIME)
		values
		( 			#{id},#{firstName},#{lastName},#{email},#{password},now()		)
	</insert>		<select id="queryByEmail" parameterType="string" resultType="int">		select count(*) from WEB_USER where EMAIL=#{email}	</select>		<!-- 前台登录 -->	<select id="frontLogin" parameterType="pd" resultType="pd">		select 			* 		from 			WEB_USER		WHERE 			EMAIL=#{email} and password = #{password}						</select>		<!-- 后台接口,站点列表 -->	<select id="listPage" resultType="pd" parameterType="page">		SELECT			WU.ID AS id,			WU.FIRST_NAME AS firstName,			WU.LAST_NAME AS lastName,			WU.EMAIL AS email,			DATE_FORMAT(WU.CREATE_TIME,'%Y-%m-%d %H:%i:%s') AS createTime		FROM			WEB_USER WU		ORDER BY			WU.CREATE_TIME DESC					<!-- <trim prefix="WHERE" prefixOverrides="AND|OR">			<if test="pd.keywords!=null and pd.keywords!=''">				WC.FIRST_NAME LIKE CONCAT(CONCAT('%', #{pd.keywords}), '%')			</if>			<if test="pd.status!=null and pd.status!=''">				AND C.STATUS = #{pd.status}			</if>		</trim> -->	</select>		<delete id="delContactUs" parameterType="pd">		delete from		WEB_CONTACTUS		where CONTACTUS_ID = #{ID}	</delete>
	<delete id="delsContactUs" parameterType="string">		DELETE FROM		WEB_CONTACTUS		WHERE CONTACTUS_ID IN				<foreach collection="array" open="(" separator="," close=")" item="item">			#{item}		</foreach>	</delete>
		<!-- 根据邮箱修改密码 -->	<update id="updatePassword" parameterType="pd">		UPDATE 			WEB_USER 		SET			PASSWORD = #{password}		WHERE 			EMAIL = #{email}	</update>	<delete id="delete" parameterType="pd">		delete from		WEB_USER		where ID = #{ID}	</delete>	<delete id="deletes" parameterType="string">		delete from		WEB_USER		where ID in				<foreach collection="array" open="(" separator="," close=")" item="item">			#{item}		</foreach>	</delete>		<!-- 根据id查询享清福 -->	<select id="detailById" parameterType="string" resultType="pd">		select			ID id,			FIRST_NAME firstName,			LAST_Name lastName,			EMAIL email,			PASSWORD password,			DATE_FORMAT(CREATE_TIME,'%Y-%m-%d %H:%i:%s') AS createTime		from			WEB_USER		where 			ID = #{id}	</select>
</mapper>