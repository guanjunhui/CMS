<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="commitPlanMapper">
	<!-- 后台接口,站点列表 -->	<select id="listPage" resultType="pd" parameterType="page">		SELECT			WCP.id,			WCP.username,			WCP.phone,			WCP.email,			WCP.company,			WCP.handle,			WCP.plan_msg AS planMsg,			DATE_FORMAT(WCP.CREATE_TIME,'%Y-%m-%d %H:%i') AS createTime		FROM			WEB_COMMIT_PLAN WCP				<trim prefix="WHERE" prefixOverrides="AND|OR">			<if test="pd.keywords!=null and pd.keywords!=''">				WCP.plan_msg LIKE CONCAT(CONCAT('%', #{pd.keywords}), '%')			</if>			<!-- <if test="pd.status!=null and pd.status!=''">				AND C.STATUS = #{pd.status}			</if> -->		</trim>		ORDER BY			WCP.CREATE_TIME DESC			</select>		<!-- 根据id删除 -->	<delete id="deleteById" parameterType="pd">		delete from		WEB_COMMIT_PLAN		where id = #{id}	</delete>		<!-- 根据ids批量删除 -->
	<delete id="deleteByIds" parameterType="string">		delete from		WEB_COMMIT_PLAN		where id in				<foreach collection="array" open="(" separator="," close=")" item="item">			#{item}		</foreach>	</delete>
		<!-- 修改处理状态 -->	<update id="updateHandle" parameterType="pd">		UPDATE			WEB_COMMIT_PLAN		SET 			handle = #{handle}		WHERE			id=#{id}	</update>		<!-- 根据id查询详情 -->	<select id="detailById" parameterType="string" resultType="pd">		SELECT			WCP.id,			WCP.username,			WCP.phone,			WCP.email,			WCP.company,			WCP.plan_msg AS planMsg,			DATE_FORMAT(WCP.CREATE_TIME,'%Y-%m-%d %H:%i:%s') AS createTime		FROM			WEB_COMMIT_PLAN WCP		WHERE			id=#{id}		ORDER BY			WCP.CREATE_TIME DESC	</select>			<!-- 新增联系方案 -->	<insert id="save" parameterType="pd">		insert into 			WEB_COMMIT_PLAN			(id,user_id,username,phone,email,company,plan_msg,create_time)		values          (#{id},#{userId},#{username},#{phone},#{email},#{company},#{planMsg},now())	</insert>		<!-- 查询所有 -->	<select id="findAll" resultType="hashmap">		select			WCP.username,			WCP.phone,			WCP.email,			WCP.company,			WCP.plan_msg AS planMsg,			DATE_FORMAT(WCP.CREATE_TIME,'%Y-%m-%d %H:%i') AS createTime		FROM			WEB_COMMIT_PLAN WCP		ORDER BY			createTime DESC	</select>
</mapper>